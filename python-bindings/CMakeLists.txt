set(PYTHON_BINDINGS_INSTALL_DIR "${CMAKE_INSTALL_PREFIX}/python-bindings")
set(PYTHON_BINDINGS_INSTALL_DIR
    "${PYTHON_BINDINGS_INSTALL_DIR}"
    PARENT_SCOPE)

install(
    DIRECTORY ${CMAKE_SOURCE_DIR}/python-bindings/REST
    DESTINATION ${PYTHON_BINDINGS_INSTALL_DIR}
    COMPONENT install)
